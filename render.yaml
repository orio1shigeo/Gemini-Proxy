services:
  - type: web
    name: google-api-proxy
    env: node
    plan: free # Or your desired plan
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18 # Or your desired Node.js version